{
  "name": "sci-map-quota-monitor",
  "version": "0.1.0",
  "description": "Cloudflare Worker that polls Mapbox usage every 6h, writes a feature flag to R2, and emails when the basemap is auto-disabled or re-enabled.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "logs": "wrangler tail",
    "manual-run": "echo 'Trigger manually: curl https://<your-worker-url>?token=$WORKER_MANUAL_TRIGGER_SECRET'"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
